/**
 * LK Design System
 *
 * Design tokens and React component library extracted from the Lexus design system.
 *
 * @example
 *   // Tokens
 *   import { darkTheme, lightTheme, spacing, typography } from '@/design-system'
 *
 *   // Components
 *   import { Button, Text, Stack, Surface } from '@/design-system'
 *
 *   // Direct component imports (tree-shakeable)
 *   import { Button } from '@/design-system/components/Button'
 */

// Design tokens
export * from './tokens'

// Components (all variants, all types)
export * from './components'
