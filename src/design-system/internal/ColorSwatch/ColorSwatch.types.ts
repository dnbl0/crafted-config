export interface ColorSwatchProps {
    /** Hex, rgb, or hsl colour value */
    color: string
    /** Display name shown below the swatch */
    name: string
    /** Size in pixels (width & height) */
    size?: number
}
